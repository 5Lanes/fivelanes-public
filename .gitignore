.env
.venv
venv
__pycache__/
*.py[cod]
.DS_Store

# OAuth config (real files only; examples stay tracked)
/credentials/*
!/credentials/credentials.example.json
!/credentials/calendar_scheduling_rules.example.json

# Dependencies
node_modules/

# Private data
/fivelanes-data/
timeline.db
