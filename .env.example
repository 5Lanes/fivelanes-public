# Bootstrap only — copy to .env in this repo root (gitignored).
# All real configuration lives in your private data directory; see README.md.
FIVELANES_DATA_ROOT=/path/to/your/fivelanes-data
# When unset, defaults to ./fivelanes-data beside this repo.
